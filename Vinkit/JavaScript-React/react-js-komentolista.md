<!-- tags: vinkit, javascript-react -->

# React JS -komentolista

> Lähde: ulkoinen materiaali (tallennettu sosiaalisesta mediasta), ei sivuston omaa sisältöä.

Kokoelma React JS -kehityksessä useimmin vastaan tulevia avainsanoja, hookeja, tapahtumakäsittelijöitä ja JS-metodeja – hyödyllinen muistilista/hakemisto.

## React-ydinkäsitteet

`React`, `Fragment`, `StrictMode`, `Suspense`, `lazy`, `memo`, `forwardRef`, `createElement`, `cloneElement`, `createContext`, `function`, `return`, `export default`, `import`, `props`, `children`, `key`, `ref`

## Hookit

`useState`, `useEffect`, `useContext`, `useRef`, `useReducer`, `useMemo`, `useCallback`, `setState`, `dispatch`

## Context

`Provider`, `Consumer`

## Tapahtumakäsittelijät (event handlers)

`onClick`, `onChange`, `onSubmit`, `onInput`, `onFocus`, `onBlur`, `onKeyDown`, `onKeyUp`, `onKeyPress`, `onMouseOver`, `onMouseOut`, `onMouseEnter`, `onMouseLeave`, `onDoubleClick`, `onScroll`, `onDrag`, `onDrop`, `onCopy`, `onPaste`

## Kontrollirakenteet

`if`, `else`, `switch`, `ternary`, `map`, `reduce`, `forEach`, `sort`, `preventDefault`, `handleSubmit`, `handleChange`

## React Router

`BrowserRouter`, `Routes`, `Route`, `Link`, `NavLink`, `Navigate`, `Outlet`, `useNavigate`, `useParams`, `useLocation`

## Renderöinti

`createRoot`, `render`, `hydrateRoot`, `findDOMNode`

## Async / verkko

`fetch`, `axios`, `async`, `await`, `find`, `then`, `catch`, `finally`, `Promise`

## DOM- ja apufunktiot

`focus`, `scrollIntoView`, `className`, `htmlFor`, `style`, `console.log`, `JSON.parse`, `JSON.stringify`, `setTimeout`, `setInterval`, `clearTimeout`, `clearInterval`
